CKEDITOR.replace('{{ ckeditor_ctn }}', {

    language: drafTerbit.locale,
    skin: "bootstrap",
    extraPlugins: 'wpmore',
    filebrowserWindowWidth  : 1040,
    filebrowserWindowHeight : 453,

    // @todo make event here to add filebrowserImageBrowseUrl
    filebrowserImageBrowseUrl : "{{ url('dt_file_browser') }}"
    // Custom config. skin and plugins is handled by assetic
    // customConfig : "{{ asset('config.js') }}",
});

// handle footer position
CKEDITOR.on("instanceReady", function(event)
{
    var h = $('.page-wrapper').height();
    var hW = $(window).innerHeight();

    if(h+30 > hW) {
        $('.footer').removeClass('navbar-fixed-bottom');
    }
});
